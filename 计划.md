# Recaping - 开发计划

## 项目概述

Recaping 是一款多媒体录音记录应用，允许用户在录音过程中同时记录照片、视频片段和文字笔记，形成完整的多媒体时间轴记录。

### 核心功能

1. **录音功能**：持续录音，自动分段存储
2. **多媒体记录**：照片、短视频片段、文字笔记
3. **时间轴展示**：双时间轴模型
4. **回放功能**：同步回放录音和多媒体内容
5. **AI 功能**：转文字、智能摘要、关键词提取

### 技术栈

- **框架**：Flutter 3.x + Dart
- **数据库**：sqflite（SQLite）
- **音频录制**：record
- **音频播放**：just_audio
- **相机**：image_picker
- **状态管理**：flutter_riverpod
- **路由**：go_router

---

## 开发阶段规划

### P0 - 项目初始化 + SQLite 数据存储层 + 录音核心功能

**目标**：搭建项目基础架构，实现核心录音功能和数据存储

#### 任务列表

- [x] **项目初始化**
  - [x] 创建 Flutter 项目
  - [x] 配置项目结构（lib/ 目录组织）
  - [x] 配置 pubspec.yaml 依赖
  - [x] 配置代码规范（analysis_options.yaml）
  - [x] 配置主题和样式

- [x] **数据库层**
  - [x] 设计数据库表结构
  - [x] 实现 DatabaseHelper（数据库管理器）
  - [x] 实现 SessionDatabase（会话表操作）
  - [x] 实现 ConfigDatabase（配置表操作）
  - [ ] 编写数据库迁移脚本
  - [ ] 数据库单元测试

- [x] **数据模型**
  - [x] Session 模型（录音会话）
  - [x] AudioChunk 模型（音频片段）
  - [x] Photo 模型（照片）
  - [x] VideoChunk 模型（视频片段）
  - [x] TextNote 模型（文字笔记）
  - [x] Bookmark 模型（书签）
  - [x] AiResult 模型（AI 结果）
  - [x] TimelineEvent 模型（时间轴事件）

- [x] **录音核心功能**
  - [x] 集成 record 包
  - [x] 实现 RecordingService
  - [x] 实现音频分段存储（15秒/段）
  - [x] 实现录音状态管理
  - [x] 录音权限处理
  - [ ] 后台录音支持

- [x] **基础 UI**
  - [x] 主页（会话列表）
  - [x] 录音页面（基础控制）
  - [x] 录音状态显示

---

### P1 - 多媒体记录 + 时间轴展示

**目标**：实现多媒体记录功能和双时间轴展示

#### 任务列表

- [x] **相机功能**
  - [x] 集成 image_picker
  - [x] 实现拍照功能
  - [x] 实现短视频录制（5秒/段）
  - [x] 相机权限处理
  - [x] 媒体文件存储管理

- [x] **文字笔记**
  - [x] 实现文字输入界面
  - [x] 文字笔记存储
  - [x] 快速添加笔记功能

- [x] **时间轴服务**
  - [x] 实现 TimelineService
  - [x] 录音时间轴管理
  - [x] 多媒体时间轴管理
  - [x] 时间轴同步机制

- [x] **时间轴 UI**
  - [x] 录音时间轴组件
  - [x] 多媒体时间轴组件
  - [x] 时间轴缩放/滚动
  - [x] 时间轴标记显示

- [x] **录音页面完善**
  - [x] 多媒体快捷按钮
  - [x] 实时时间轴预览
  - [x] 录音波形显示

---

### P2 - 回放功能 + 文件管理

**目标**：实现完整的回放功能和文件管理系统

#### 任务列表

- [x] **音频播放**
  - [x] 集成 just_audio
  - [x] 实现 AudioPlaybackService
  - [x] 音频片段无缝衔接播放
  - [x] 播放控制（播放、暂停、跳转）

- [x] **回放页面**
  - [x] 回放页面 UI
  - [x] 双时间轴联动播放
  - [x] 多媒体内容同步显示
  - [x] 播放进度控制

- [x] **书签功能**
  - [x] 添加书签
  - [x] 书签列表
  - [x] 跳转到书签位置

- [x] **文件管理**
  - [x] .recp 文件格式实现
  - [x] 会话导出功能
  - [x] 会话导入功能
  - [x] 文件分享功能

  - [x] **存储管理**
  - [x] 存储空间统计
  - [x] 清理过期文件
  - [x] 压缩媒体文件

---

### P3 - AI 功能 + 设置 + 优化

**目标**：集成 AI 功能，完善设置页面，性能优化

#### 任务列表

- [x] **AI 功能**
  - [x] 集成语音识别（OpenAI 兼容 API）
  - [x] 录音转文字功能
  - [x] 智能摘要生成
  - [x] 会议记录生成
  - [x] AI 结果存储和展示
  - [ ] 关键词提取

- [x] **设置页面**
  - [x] 录音设置（音质、分段时长）
  - [x] 存储设置
  - [x] 主题设置
  - [x] 关于页面

- [ ] **性能优化**
  - [ ] 列表性能优化
  - [ ] 图片加载优化
  - [ ] 内存管理优化
  - [ ] 启动速度优化

- [ ] **测试与发布**
  - [ ] 单元测试覆盖
  - [ ] 集成测试
  - [ ] UI 测试
  - [ ] Android 发布准备
  - [ ] iOS 发布准备

---

## 当前阶段：P0-P2 核心功能已完成 ✅

### P0 详细任务分解

#### Week 1: 项目初始化

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 创建 Flutter 项目 | `flutter create recaping` | P0 |
| 配置目录结构 | 按架构设计创建目录 | P0 |
| 添加依赖 | record, sqflite, path_provider 等 | P0 |
| 配置分析选项 | analysis_options.yaml | P1 |
| 主题配置 | 定义颜色、字体等 | P1 |

#### Week 2: 数据库层

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 数据库设计 | 设计所有表结构 | P0 |
| DatabaseManager | 数据库连接管理 | P0 |
| SessionDatabase | 会话表 CRUD | P0 |
| ConfigDatabase | 配置表 CRUD | P0 |
| 迁移脚本 | 版本升级支持 | P1 |

#### Week 3: 数据模型

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| Session 模型 | 录音会话数据类 | P0 |
| AudioChunk 模型 | 音频片段数据类 | P0 |
| Photo 模型 | 照片数据类 | P1 |
| VideoChunk 模型 | 视频片段数据类 | P1 |
| TextNote 模型 | 文字笔记数据类 | P1 |

#### Week 4: 录音功能

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| RecordingService | 录音服务核心 | P0 |
| 音频分段 | 15秒自动分段 | P0 |
| 状态管理 | 录音状态 Riverpod | P0 |
| 权限处理 | 录音权限申请 | P0 |
| 后台录音 | 后台持续录音 | P1 |

---

## 技术决策记录

### 已决策

| 日期 | 决策 | 说明 |
|-----|------|------|
| 2026-04-09 | 使用 Flutter | 跨平台开发，一套代码双端运行 |
| 2026-04-09 | 使用 sqflite | Flutter 官方推荐的 SQLite 方案 |
| 2026-04-09 | 使用 record | 简单易用，支持分段录音 |
| 2026-04-09 | 使用 Riverpod | 类型安全，性能优秀 |

### 待决策

| 问题 | 选项 | 状态 |
|-----|------|------|
| AI 语音识别 | 本地模型 vs 云端 API | 待调研 |
| 视频录制 | 自定义相机 vs image_picker | 待验证 |
| 文件导出 | 纯 SQLite vs ZIP 打包 | 待设计 |

---

## 风险与问题

| 风险 | 影响 | 缓解措施 |
|-----|------|---------|
| 后台录音限制 | 高 | 研究 Android/iOS 后台执行策略 |
| 音频同步问题 | 中 | 精确时间戳，充分测试 |
| 存储空间不足 | 中 | 定期清理，压缩媒体 |
| 性能问题 | 中 | 懒加载，分页查询 |

---

## 更新日志

### 2026-04-09
- 创建开发计划文档
- 定义 P0-P3 开发阶段
- 分解 P0 阶段详细任务
- 完成 Flutter 项目初始化（Flutter 3.41.6）
- 配置所有依赖并成功运行 flutter pub get
- 创建基础目录结构和应用入口骨架
- 配置 Android/iOS 权限

### 2026-04-09（数据模型层 + 数据存储层）
- 实现应用常量定义（AppConstants）
- 实现 8 个数据模型（Session, AudioChunk, VideoChunk, Photo, TextNote, Bookmark, AiResult, TimelineEvent）
- 实现 DatabaseHelper 数据库辅助类（单例模式，管理连接和目录）
- 实现 SessionDatabase 会话数据库操作类（完整 CRUD + 聚合查询 + 统计）
- 实现 ConfigDatabase 全局配置数据库（配置项 CRUD + AI API 配置 + 会话摘要）
- 删除所有 .gitkeep 文件
- flutter analyze 通过（0 issues）

### 2026-04-09（核心服务层）
- 实现 ThumbnailUtil 缩略图生成工具
- 实现 RecordingService 录音服务（双时间轴、15秒自动分段、record 包集成）
- 实现 TimelineService 时间轴服务（照片/笔记/书签 CRUD、时间轴事件聚合查询）
- 实现 StorageService 存储管理服务（会话创建/删除/搜索、.recp 文件管理、存储统计）
- 实现 AudioPlaybackService 音频回放服务（音频分片合并、just_audio 播放/暂停/跳转/变速）
- 实现 CameraService 相机服务（拍照/录像/相册选择，封装 image_picker）
- flutter analyze 通过（0 issues）

### 2026-04-09（状态管理层 + 路由配置 + 页面占位）
- 实现录音状态管理（recording_provider.dart）
  - RecordingService/CameraService Provider
  - 录音状态/计时 StreamProvider
  - TimelineEventsNotifier 时间轴事件管理
  - RecordingControlNotifier 录音控制（开始/暂停/继续/停止/拍照/笔记/书签）
- 实现会话列表状态管理（session_provider.dart）
  - StorageService FutureProvider
  - SessionListNotifier 会话列表 CRUD + 搜索
  - StorageStatsNotifier 存储统计
- 实现回放状态管理（playback_provider.dart）
  - AudioPlaybackService Provider
  - 播放状态/位置 StreamProvider
  - PlaybackSpeedNotifier 播放速度
  - PlaybackControlNotifier 回放控制（加载/播放/暂停/跳转/事件跳转）
- 实现设置状态管理（settings_provider.dart）
  - ConfigDatabase FutureProvider
  - ThemeModeNotifier 主题模式（持久化）
  - AudioSettingsNotifier 录音设置（持久化）
  - AiSettingsNotifier AI API 设置（持久化）
- 配置 go_router 路由（6 条路由）
- 更新 RecapingApp 使用 MaterialApp.router + ProviderScope + Consumer
- 创建 5 个页面占位文件（首页/录音/回放/设置/AI）
- 使用手动 Provider 定义，不依赖代码生成
- flutter analyze 通过（0 issues）

### 2026-04-09（会话列表首页 UI）
- 创建日期格式化工具（DateFormatUtil）：时长格式化、日期时间格式化、相对时间格式化
- 创建会话卡片组件（SessionCard）：缩略图、标题、时间、时长、事件数、标签、Slidable 滑动删除
- 实现完整的会话列表首页（HomePage）：
  - AppBar（标题 + 搜索按钮 + 设置按钮 + 可展开搜索栏）
  - 会话列表（ListView + SessionCard + 下拉刷新）
  - 空状态 / 错误状态 / 加载状态
  - FAB（开始录音）
  - 长按底部菜单（编辑标题、删除、查看详情）
  - 编辑标题对话框、删除确认对话框
  - 页面初始化自动加载会话列表
- flutter analyze 通过（0 issues）

### 2026-04-09（录音页面 UI + 组件）
- 创建录音控制组件：
  - WaveformIndicator 波形动画指示器（CustomPaint + AnimationController）
  - RecordingControls 录音控制按钮组（录音/暂停/停止）
- 创建时间轴组件：
  - TimelineEventItem 时间轴事件项（照片/视频/笔记/书签四种类型展示）
  - RecordingTimeline 录音实时时间轴（自动滚动到底部）
- 实现完整录音页面（RecordPage）：
  - AppBar（返回确认、可编辑标题、完成按钮）
  - 顶部状态区域（脉冲动画指示器、双时间轴时间显示、波形指示器）
  - 中部实时时间轴（RecordingTimeline）
  - 底部操作栏（拍照/录视频/笔记/书签快捷按钮 + 录音控制按钮）
  - 文字笔记底部 Sheet（标题 + 内容输入）
  - 书签底部 Sheet（标签 + 8 种预设颜色选择）
  - 放弃录音/停止录音确认对话框
  - PopScope 拦截返回按钮
  - Material 3 设计风格，支持深色模式
- flutter analyze 通过（0 issues）

### 2026-04-09（回放页面 UI + 组件）
- 创建图片查看组件（ImageViewer）：全屏查看、InteractiveViewer 缩放/平移
- 创建音频播放控制组件（AudioPlayerControls）：进度条、播放/暂停、快退/快进、速度切换
- 创建播放时间轴组件（PlaybackTimeline）：播放进度同步高亮、自动滚动、事件点击跳转
- 实现完整回放页面（PlaybackPage）：
  - 会话信息卡片（可折叠）、时间轴视图、底部播放控制栏
  - 事件详情弹窗（照片全屏查看、笔记底部 Sheet、书签对话框）
  - 更多操作菜单（编辑标题、删除会话、AI 功能）
  - 加载中/错误状态处理
  - Material 3 设计风格，支持深色模式
- flutter analyze 通过（0 issues）

### 2026-04-09（设置页面 UI）
- 实现完整设置页面（SettingsPage）：
  - 外观设置：SegmentedButton 切换亮色/暗色/跟随系统主题
  - 录音设置：音频格式（AAC 只读）、采样率选择（44100Hz/22050Hz）、声道数选择（单声道/双声道）
  - AI 配置：API Base URL、API Key（密码样式）、模型名称、保存按钮
  - 存储管理：存储空间统计、会话数量、清除缓存
  - 关于：应用名称、版本号、作者、开源许可
- Material 3 设计风格，支持深色模式
- 所有设置项持久化到数据库
- flutter analyze 通过（0 issues）

### 2026-04-09（AI 功能页面 + AI 服务）
- 添加 http 依赖（^1.2.0）到 pubspec.yaml
- 创建 AI 服务（AiService）：语音转文字、摘要生成、会议记录生成、API 连接测试
- 创建 AI Provider：AI 服务实例、任务状态管理、结果列表（带刷新）
- 实现完整 AI 功能页面（AiPage）：
  - API 未配置提示（图标 + 文字 + 前往设置按钮）
  - AI 功能入口卡片（语音转文字/会议摘要/会议记录）
  - 处理进度遮罩（CircularProgressIndicator + 文字提示）
  - AI 结果列表（类型标签、可展开文本、复制/删除操作）
- Material 3 设计风格，主色调 Color(0xFF6B6BFF)，支持深色模式
- flutter analyze 通过（0 issues）

### 2026-04-09（短视频录制功能）
- 实现 TimelineService.addVideo 方法：接收视频数据，创建 VideoChunk 存入数据库
- 实现 RecordingControlNotifier.recordVideo 方法：调用相机录制视频并添加到时间轴
- 启用录音页面录视频按钮：录音进行中可用，调用 recordVideo()
- 简化处理：整个视频作为单个 VideoChunk 存储，后续可优化为真正的 5 秒分段
- flutter analyze 通过（0 issues）

### 2026-04-09（会话导出/导入/分享功能）
- 添加 share_plus (^7.2.1) 和 file_picker (^6.1.1) 依赖
- 创建 ExportService 导出导入服务：
  - exportSession：将 .recp 文件复制到临时目录
  - shareSession：使用系统分享面板分享 .recp 文件（支持友好文件名）
  - importSession：file_picker 选择文件 → SQLite 文件头验证 → 数据库结构验证 → 复制到 sessions 目录 → 保存全局摘要
- 更新 SessionListNotifier：添加 importSession 方法
- 更新首页：AppBar 添加导入按钮（Icons.file_download）
- 更新回放页面：更多操作菜单添加「导出」和「分享」菜单项
- flutter analyze 通过，0 issues

### 2026-04-09（时间轴缩放/滚动功能）
- 创建可缩放时间轴组件（ZoomableTimeline）：三种缩放级别（compact/normal/detailed）、顶部缩放按钮组
- 更新时间轴事件项组件（TimelineEventItem）：添加 zoomLevel 和 isHighlighted 参数，根据缩放级别调整显示
- 更新录音时间轴组件（RecordingTimeline）：集成 ZoomableTimeline 包装器
- 更新回放时间轴组件（PlaybackTimeline）：集成缩放控制，缩放时保持当前播放位置可见
- flutter analyze 通过（0 issues）

### 2026-04-09（清理过期文件和压缩媒体文件功能）
- 更新 StorageService：添加 clearTempFiles、cleanOldSessions、compressSessionMedia、getDetailedStorageStats 方法
- 更新 StorageStatsNotifier：使用 getDetailedStorageStats 获取详细分类统计
- 更新设置页面：存储统计增强（分类占比条形图）、清除临时文件、清理旧会话、压缩媒体三个操作按钮
- 所有操作均有确认对话框，操作完成后显示结果
- flutter analyze 通过（0 issues）

### 2026-04-09（用户反馈问题修复）
- **问题1：照片太糊** - 移除 `CameraService.takePhoto()` 和 `pickFromGallery()` 中的 `maxWidth/maxHeight/imageQuality` 压缩参数，保留原图质量
- **问题2：录音没有显示在时间轴上** - 在 `TimelineEventType` 新增 `audio` 类型，`SessionDatabase.getTimelineEvents()` 查询 audio_chunks 生成录音区间事件，`TimelineEventItem` 新增麦克风图标+时长显示样式
- **问题3：总时间轴暂停功能** - `RecordingState` 新增 `totalPaused` 状态，`RecordingService` 新增 `pauseTotalTimeline()/resumeTotalTimeline()` 方法，录音页面新增暂停按钮和锁定状态UI，暂停时禁用所有快捷操作
- flutter analyze 通过（0 issues）

### 2026-04-09（修复 file_picker 编译错误）
- 修复 file_picker 6.2.1 与 Flutter 3.41.6 不兼容的编译错误
- 升级 file_picker 从 6.2.1 到 11.0.2
- 更新 export_service.dart 中 API 调用：`FilePicker.platform.pickFiles()` → `FilePicker.pickFiles()`
- flutter analyze 通过（0 issues）
- flutter build apk --debug 构建成功

### 2026-04-09（存储方案重构 — 文件系统存储 + 单文件导出导入）
- **背景**：原方案将所有数据作为 BLOB 存入 SQLite，导致大照片触发 "Row too big" 错误，数据库文件过大
- **新方案**：每个会话一个目录，数据库只存元数据和文件路径引用，媒体文件存储在子目录中
- **数据模型重构**：AudioChunk/Photo/VideoChunk/TimelineEvent 的 BLOB 字段全部改为文件路径引用
- **数据库重构**：建表 SQL 中 BLOB 字段改为 TEXT（file_path/thumbnail_path）；SessionDatabase 新增 resolvePath() 方法
- **DatabaseHelper 重构**：新增 sessionDirPath()/ensureSessionDirs()/deleteSessionDirectory() 方法；数据库文件改为会话目录内的 session.db
- **服务层重构**：
  - StorageService：创建/删除会话目录、目录大小统计
  - RecordingService：录音分片保存到文件系统
  - TimelineService：照片/视频写入文件系统
  - AudioPlaybackService：从文件系统读取音频合并播放
  - ExportService：使用 archive 包 ZIP 打包/解压
- **UI 组件更新**：缩略图从 Image.memory() 改为 Image.file()，ImageViewer 支持文件路径
- **AI 页面更新**：音频数据从文件系统读取合并
- 添加 archive: ^3.4.10 依赖
- flutter analyze 通过（0 issues）

---

## 项目完成总结

### 完成日期

2026-04-09

### 已完成阶段

| 阶段 | 描述 | 状态 |
|------|------|------|
| P0 | 项目初始化 + SQLite 数据存储层 + 录音核心功能 | ✅ 已完成 |
| P1 | 多媒体记录 + 时间轴展示 | ✅ 已完成 |
| P2 | 回放功能 + 文件管理 | ✅ 已完成 |
| P3 | AI 功能 + 设置 + 优化 | ✅ AI 功能和设置已完成（性能优化和测试待进行） |

### 项目统计

- **Dart 源文件**：42 个
- **代码行数**：11,504 行
- **flutter analyze**：0 issues

### P1/P2 完成总结

#### P1 阶段完成功能
- **短视频录制**：集成 image_picker 录制视频，通过 TimelineService 添加到时间轴
- **时间轴缩放**：ZoomableTimeline 组件，支持 compact/normal/detailed 三种缩放级别
- **时间轴事件项增强**：根据缩放级别调整显示样式，支持回放高亮效果

#### P2 阶段完成功能
- **导出/导入/分享**：ExportService 支持 .recp 文件导出、系统分享面板分享、file_picker 导入（含文件验证）
- **清理过期文件**：clearTempFiles 清除临时文件、cleanOldSessions 按天数清理旧会话
- **压缩媒体文件**：compressSessionMedia 重新生成更小缩略图以节省空间
- **存储统计增强**：分类统计音频/照片/视频占用空间，分段条形图展示

### 后续迭代计划（P3+ 阶段）

#### 短期优化
1. **后台录音支持**：Android Foreground Service + iOS Background Audio
2. **短视频录制完善**：5 秒自动分段录像功能
3. ~~**文件导出/导入**：.recp 文件格式实现、ZIP 打包导出~~ ✅ 已完成（基础导出/导入/分享）
4. **数据库迁移脚本**：版本升级支持
5. **关键词提取**：AI 关键词提取功能

#### 中期优化
6. **性能优化**：列表懒加载、图片缓存、内存管理、启动速度
7. **测试覆盖**：核心业务逻辑单元测试、关键流程集成测试
8. ~~**时间轴缩放/滚动**：手势缩放和自由滚动支持~~ ✅ 已完成

#### 长期规划
9. **发布准备**：Android 签名 + ProGuard、iOS 证书 + App Store 审核
10. **国际化（i18n）**：多语言支持
11. **无障碍支持**：Accessibility 优化
12. **云端同步**：数据备份和跨设备同步

### 2026-04-10（用户反馈 Bug 修复 — 第二轮）
- **Bug 1：时间轴显示每段独立录音** - 修改 `SessionDatabase.getTimelineEvents()` 将每个 audio_chunk 作为独立事件返回，`TimelineEvent.fromAudio()` 新增 `label` 参数显示 "录音 #1"、"录音 #2" 等
- **Bug 2：修复 "Bad state" 错误** - 修改 `RecordingService.stopSession()` 避免重复调用 `_recorder.stop()`，`recording` 状态下仅通过 `_stopAndSaveCurrentChunk()` 停止，`paused`/`totalPaused` 状态单独处理并用 try-catch 包裹
- **Bug 3：照片和视频使用系统查看器** - 添加 `open_filex` 依赖，`TimelineEvent` 新增 `mediaFilePath` 字段，`PlaybackPage` 的照片/视频点击改为调用 `OpenFilex.open()` 打开系统查看器
- flutter analyze 通过（0 issues）

### 2026-04-10（PR风格时间轴重构）
- **时间轴重新设计** - 类似Premiere Pro等视频剪辑软件的时间轴风格
- **新增组件**：
  - `timeline_ruler.dart` - 时间刻度尺组件（横向/竖向）
  - `timeline_track.dart` - 轨道组件（录音/照片/视频/笔记/书签轨道）
  - `timeline_playhead.dart` - 播放头组件
- **重构 playback_timeline.dart** - 整合新组件，左侧显示时间刻度，顶部显示轨道标签，录音段显示为连续绿色块，叠加播放头指示
- **更新 playback_page.dart** - 传递总时长参数
- **设计特点**：
  - 保持竖向布局
  - 左侧显示完整时间刻度（MM:SS格式）
  - 录音分段显示为独立绿色块，标注标签和时长
  - 红色播放头实时指示当前位置
  - 轨道分类标签显示事件数量
  - 支持三种缩放级别
- flutter analyze 通过（0 issues）

### 2026-04-10（Bug 修复 - `_dependents.isEmpty` 断言错误）
- **问题**：点击保存按钮时出现 Flutter 框架断言错误 `_dependents.isEmpty`
- **原因**：`stopSession()` 异步执行过程中，`RecordingTimeline` Widget 正在被 dispose，但 Provider 的状态更新尝试通知正在 dispose 的 Widget，导致依赖关系冲突
- **修复内容**：
  - `lib/providers/recording_provider.dart`：
    - `stopSession()` 添加多处 `mounted` 检查（异步操作前后）
    - `clear()` 添加 try-catch 保护
  - `lib/widgets/timeline/recording_timeline.dart`：
    - `_scrollToBottom()` 添加 `mounted` 检查
- flutter analyze 通过（0 issues）

### 2026-04-10（Bug 修复 - PlaybackPage "Bad state: No element" 错误）
- **问题**：保存录音后导航到 PlaybackPage 时出现 `StateError: Bad state: No element` 错误
- **原因**：
  - 时序问题：`sessionListProvider` 可能还没刷新完成，新会话不在列表中
  - `firstWhere` 缺少默认值处理：找不到元素时抛出 `StateError`
- **修复内容**：
  - `lib/pages/playback/playback_page.dart`：
    - 4处 `firstWhere` 调用全部添加 `orElse: () => null` 参数（第149、297、724、743行）
- flutter analyze 通过（0 issues）

---

### 2026-04-10（修改录音保存后的导航逻辑）
- **需求**：录制完成后点击保存按钮应返回首页，而非跳转到回放页面
- **修改内容**：
  - `lib/pages/record/record_page.dart`：
    - `_showStopConfirmDialog()` 导航逻辑从 `go('/playback/$sessionId')` 改为 `go('/')`
    - 对话框提示文字更新为"结束后将保存录音并返回首页"
    - 移除不再使用的 `sessionId` 变量
- flutter analyze 通过（0 issues）

### 2026-04-10（GoRouter 导航返回错误修复）
- **问题**：点击左上角返回按钮时控制台输出 `GoError: There is nothing to pop`，返回操作导致应用退出
- **原因**：`HomePage` 使用 `context.go()` 导航会替换路由而非 push，导致导航栈为空
- **修复内容**：
  - `lib/pages/home/home_page.dart`：将 `go()` 改为 `push()`（设置页、回放页、录音页）
  - `lib/pages/settings/settings_page.dart`：AppBar 返回按钮添加 `canPop()` 检查
  - `lib/pages/playback/playback_page.dart`：AppBar 返回按钮添加 `canPop()` 检查
  - `lib/pages/ai/ai_page.dart`：AppBar 返回按钮添加 `canPop()` 检查
- flutter analyze 通过（0 issues）

### 2026-04-10（修复录制状态管理问题）
- **问题**：录制完成后再次录制会报错 "操作失败:Bad state: Already in a recording session"
- **原因**：`stopSession()` 将状态设置为 `stopped`，但 `startSession()` 只允许在 `idle` 状态下启动新会话，状态机缺少从 `stopped` → `idle` 的转换路径
- **修复内容**：
  - `lib/services/recording_service.dart`：将 `stopSession()` 方法中的 `_setState(RecordingState.stopped)` 改为 `_setState(RecordingState.idle)`
  - 更新注释说明重置为 `idle` 状态是为了允许再次启动新会话
- **验证场景**：
  - 录制 → 停止 → 再次录制 ✅
  - 录制 → 暂停 → 停止 → 再次录制 ✅
  - 录制 → 总暂停 → 停止 → 再次录制 ✅
- flutter analyze 通过（0 issues）

---

### 2026-04-10（修复录音完成后返回首页不自动刷新的问题）
- **问题**：录音完成返回首页后需要手动刷新才能看到新的录音
- **原因**：`stopSession()` 方法没有调用 `sessionListProvider.notifier.loadSessions()` 刷新会话列表
- **修复内容**：
  - `lib/providers/recording_provider.dart`：
    - 在 `stopSession()` 方法中添加 `_ref.read(sessionListProvider.notifier).loadSessions();` 调用
    - 在清理时间轴事件之后、设置 state 之前添加，保持 `mounted` 检查模式一致
- flutter analyze 通过（0 issues）

### 2026-04-10（真实音频振幅波形显示）
- **需求**：用户反馈波形指示器使用随机数模拟，期望看到真实音频振幅波形
- **实现内容**：
  - `lib/services/recording_service.dart`：新增 `AudioAmplitude` 类和振幅流支持，使用 `record` 包的 `onAmplitudeChanged()` 获取实时振幅
  - `lib/providers/recording_provider.dart`：新增 `amplitudeProvider` 暴露振幅数据给 UI
  - `lib/widgets/recording_controls/waveform_indicator.dart`：修改为接收真实振幅数据驱动波形高度
  - `lib/pages/record/record_page.dart`：订阅振幅 Provider 并传递给波形组件
- **技术要点**：
  - 振幅数据为 dBFS 值（-60dB ~ 0dB），转换为 0.0 ~ 1.0 的可视化高度
  - 振幅更新频率 100ms，与 UI 刷新频率匹配
  - 波形显示有平滑过渡动画，中间位置高度更接近振幅值，两侧逐渐降低
- flutter analyze 通过（0 issues）

---

### 2026-04-10（修复 TextEditingController 生命周期问题）
- **问题**：添加书签或文字笔记时报 `TextEditingController was used after being disposed` 错误
- **原因**：在 `Navigator.pop(context)` 后通过 `.then()` 回调获取 TextEditingController 的值，此时控制器已被 dispose
- **修复内容**：
  - `lib/pages/record/record_page.dart`：
    - `_showTextNoteBottomSheet()`：在关闭 Sheet 前获取 `content` 和 `title` 的值，移除手动 dispose
    - `_showBookmarkBottomSheet()`：在关闭 Sheet 前获取 `label` 的值，移除手动 dispose
- **技术要点**：TextEditingController 由 BottomSheet State 管理，会自动释放；应在关闭前获取值，避免异步回调访问已释放资源
- flutter analyze 通过（0 issues）

---

---

### 2026-04-29（修复录音时间轴两个 Bug）

- **Bug 1：暂停总时间轴时时间轴异常延伸** - `pauseTotalTimeline()` 不再调用 `_updateSegmentsOnPause()`，暂停总时间轴不影响录音段
- **Bug 2：恢复时间轴后录音状态不正确** - `resumeTotalTimeline()` 不再调用 `_updateSegmentsOnResume()`，恢复总时间轴时保持录音的原始状态
- **修复原则**：录音段的变更只由录音本身的操作触发（`startSession`/`pauseRecording`/`resumeRecording`/`stopSession`），暂停/恢复总时间轴不影响录音段和录音状态
- 修改文件：`lib/providers/recording_provider.dart`
- flutter analyze 通过（0 issues）

---

### 2026-04-29（修复暂停总时间轴时时间跳跃的 Bug）
- **问题**：录音约3秒时暂停总时间轴，时间突然跳到7秒，继续后恢复正确时间
- **根本原因**：`RecordingService.pauseTotalTimeline()` 中计时器取消太晚，在 `_stopAndSaveCurrentChunk()` 异步文件 I/O 操作期间（约4秒），`_tickTimer` 仍在运行并持续更新 `_totalElapsedMs`，导致时间跳跃
- **修复**：将 `_tickTimer?.cancel()` 和 `_totalElapsedBeforePause` 保存移到异步操作之前，确保计时器在异步操作期间不会继续更新时间
- 修改文件：`lib/services/recording_service.dart`
- flutter analyze 通过（0 issues）

---

### 2026-04-29（修复暂停总时间轴后创建的事件显示到未来位置的 Bug）
- **问题**：暂停总时间轴后恢复，创建新事件（拍照、笔记、书签）显示在时间轴"未来"位置
- **根本原因**：事件时间戳使用挂钟时间（`DateTime.now()`），而录音时间轴使用可暂停的 `totalElapsedMs`，暂停恢复后两者不一致
- **修复内容**：
  - `lib/services/timeline_service.dart`：`addPhoto()`/`addVideo()`/`addTextNote()`/`addBookmark()` 新增可选 `timestamp` 参数
  - `lib/providers/recording_provider.dart`：所有事件创建方法传入 `recordingService.totalElapsedMs` 作为时间戳
- flutter analyze 通过（0 issues）

---

### 2026-04-29（修复3个录音相关问题）
- **Bug 1：录音中返回放弃修改后主页不显示开始录音按钮** - `record_page.dart` 的 `_showDiscardDialog()` 中 `stopSession()` 改为 `await`，添加 `mounted` 检查，确保录音状态完全重置后再返回
- **Bug 2：暂停录音+暂停时间轴后恢复显示错误按钮** - `recording_service.dart` 新增 `_stateBeforeTotalPause` 字段记录总暂停前状态，`resumeTotalTimeline()` 根据之前状态恢复正确的录音状态（`recording` 或 `paused`）
- **功能3：新增时间轴缩放按钮** - `recording_timeline.dart` 移除 `ZoomableTimeline` 包装器，改为直接管理缩放级别，新增右下角浮动缩放按钮组（`+`/`-` 按钮 + 级别标签），支持3个缩放级别
- flutter analyze 通过（0 issues）

### 2026-04-29（修复 ref.listen 在 initState 中使用导致的崩溃）

- **问题**：点击"开始录音"按钮后应用崩溃，`ref.listen` 在 `initState()` 中使用触发 `debugDoingBuild` 断言
- **原因**：`ref.listen` 只能在 `build()` 方法中使用，`initState()` 中需要使用 `ref.listenManual`
- **修复内容**：
  - `lib/pages/record/record_page.dart`：
    - 将 `ref.listen` 改为 `ref.listenManual`
    - 新增 `_controlStateSubscription` 字段存储订阅
    - `dispose()` 中调用 `_controlStateSubscription?.close()` 清理
- flutter analyze 通过（0 issues）


### 2026-04-29（缩放按钮扇形仪表盘）
- **需求**：点击缩放倍率标签时弹出扇形仪表盘，支持拖动精细调整缩放倍率
- **修改 `recording_timeline.dart`**：
  - 添加 `dart:math` 导入和 `SingleTickerProviderStateMixin`
  - 新增 `_showZoomGauge` 状态和 `_gaugeAnimController` 动画控制器
  - 缩放标签包裹 `GestureDetector`，点击切换仪表盘
  - 新增 `_FanGaugePainter`（CustomPainter）：四分之一圆弧（90°），从右侧（1x）到上方（512x），对数刻度映射
  - 新增手势处理：`_handleGaugePanStart/Update`、`_handleGaugeTap`、`_updateGaugeFromPosition`
  - 仪表盘弹出/关闭带缩放+透明度动画（200ms）
  - 点击仪表盘外部关闭
- flutter analyze 通过（0 issues）

---

### 2026-04-29（录音时间轴重新设计 — 纵向持续延伸方案）
- **需求**：录音页面时间轴在只有录音时为空，录音和事件混在一起，录音过程中没有实时显示覆盖时间。用户要求左侧纵向时间轴一直显示并不断向下延伸
- **新建 `recording_segment.dart`**：录音段数据模型（startMs/endMs/isRecording），支持 copyWith、durationMs、==和hashCode
- **修改 `recording_provider.dart`**：
  - 新增 `recordingSegmentsProvider`（StateProvider<List<RecordingSegment>>）
  - `startSession()` 重置并添加第一个录音段
  - `pauseRecording()` 关闭录音段并添加暂停段
  - `resumeRecording()` 关闭暂停段并添加录音段
  - `pauseTotalTimeline()`/`resumeTotalTimeline()` 不影响录音段（Bug 修复后）
  - `stopSession()` 清空录音段列表
- **重写 `recording_timeline.dart`**（纵向持续延伸方案）：
  - 左侧时间标签列（0:00, 0:05, 0:10, ...），根据缩放级别自动调整刻度密度
  - 中间彩色时间轴线（绿色=录音中，红色=暂停间隔），段边界显示圆点
  - 右侧事件标记卡片（照片/视频/笔记/书签），通过水平连接线与时间轴关联
  - 当前位置显示蓝色发光圆点指示器
  - 自动滚动到当前时间位置（仅在用户位于底部附近时触发）
  - 支持三种缩放级别（compact: 30dp/s, normal: 60dp/s, detailed: 120dp/s）
  - 使用 `SingleChildScrollView` + `Stack` + `Positioned` 实现高性能布局
- **修改 `record_page.dart`**：监听录音段 Provider，本地计算显示段（延伸最后一个段到当前时间），传递给新组件
- **性能优化**：在 build() 中本地计算显示段，避免额外 Provider 状态更新
- flutter analyze 通过（0 issues）

### 2026-04-29（录音时间轴连续缩放改造）
- **需求**：将固定3档缩放改为连续缩放，支持300倍缩放范围（30dp/s → 0.1dp/s）
- **修改 `recording_timeline.dart`**：
  - 移除 `TimelineZoomLevel` 枚举依赖，改为 `double _pixelsPerMs` 连续值
  - 缩放范围：`_maxPixelsPerMs = 0.03`（1x，最宽松）到 `_minPixelsPerMs = 0.0001`（300x，最紧凑）
  - 每次缩放按2倍步进（约8个级别）
  - 缩放标签显示倍率（"1x", "2x", ... "300x"）
  - 事件卡片根据 `_pixelsPerMs` 阈值自动切换显示模式
  - 刻度间隔新增30分钟和1小时，支持高缩放级别
- flutter analyze 通过（0 issues）

### 2026-04-29（修复扇形仪表盘方向错误）
- **问题**：扇形仪表盘弧线朝右超出屏幕，应朝左（屏幕内部）展开
- **修改 `recording_timeline.dart`**：
  - `_FanGaugePainter.paint()`：弧线从左侧（π）顺时针到上方（3π/2），刻度/滑块/标签位置相应调整
  - `_updateGaugeFromPosition()`：使用屏幕坐标系 `atan2(dy, dx)`，弧线范围 `[-π, -π/2]`
  - "1x" 标签移到左侧，"512x" 标签保持上方
- flutter analyze 通过（0 issues）

### 2026-04-29（深入修复录音中点击名称重命名时崩溃的 `_dependents.isEmpty` 错误）

- **问题**：之前的修复（`ref.listen` → `ref.listenManual`）未能彻底解决，录音中点击名称重命名仍崩溃
- **深入根因**：`_showEditTitleDialog()` 确认按钮中 `setState` 在 `Navigator.pop` 之前调用。`setState` 触发页面 `build()` 重建（7 个 `ref.watch` 重新注册 InheritedWidget 依赖），而 dialog 正被移除（元素 deactivate），导致 `_dependents.isEmpty` 断言失败
- **修复内容**：
  - `lib/pages/record/record_page.dart`：
    - `_showEditTitleDialog()` 用 `WidgetsBinding.instance.addPostFrameCallback` 包装，延迟到帧结束后显示 dialog
    - 确认按钮仅执行 `Navigator.pop(context)`，`setState` 移到 `.then()` 回调（dialog 完全关闭后）执行
    - `editController.dispose()` 保留在 `.then()` 中，与 `setState` 在同一生命周期位置
- flutter analyze 通过（0 issues）

*最后更新：2026-04-29 by GDNDZZK*

### 2026-04-29（修复快速暂停/继续录音时的文件操作竞态条件）
- **问题**：快速点击暂停/继续录音时出现 `PathNotFoundException`，因异步文件操作并发执行导致竞态条件
- **修复内容**：
  - `lib/services/recording_service.dart`：
    - 新增 `_operationLock`（Completer）操作锁，防止并发操作
    - `pauseRecording()`/`resumeRecording()`/`pauseTotalTimeline()`/`stopSession()`/`_rotateChunk()` 均加锁保护
    - `_stopAndSaveCurrentChunk()` 增强错误处理：立即清空路径防重复、try-catch 包裹 recorder.stop()、目标文件已存在先删除、rename 失败回退 copy+delete
  - `lib/providers/recording_provider.dart`：
    - 新增防抖机制（500ms 最小操作间隔），`pauseRecording()` 和 `resumeRecording()` 添加防抖检查
- flutter analyze 通过（0 issues）

### 2026-04-29（修复录音中点击名称重命名时崩溃）

- **问题**：录音过程中点击左上角录音名称进行重命名操作时，应用崩溃并显示 `_dependents.isEmpty` 断言错误
- **原因**：`ref.listen` 在 `build()` 中每次重建时重复注册，录音期间高频重建（StreamProvider 每 100ms 更新）导致 Riverpod 内部依赖追踪异常，触发 `_dependents.isEmpty` 断言
- **修复内容**：
  - `lib/pages/record/record_page.dart`：
    - 将 `ref.listen<AsyncValue<void>>(recordingControlProvider, ...)` 从 `build()` 移到 `initState()`，只在初始化时注册一次
    - 在 `_showEditTitleDialog()` 对话框确认按钮回调中添加 `mounted` 检查
- flutter analyze 通过（0 issues）

### 2026-04-29（最终修复：使用 Overlay 替代 showDialog 解决 `_dependents.isEmpty` 错误）

- **问题回顾**：之前的多次修复（将 `ref.listen` 移到 `initState`、`addPostFrameCallback` + `setState` 移到 `.then()`、添加全面日志分析）均未能彻底解决问题。根本原因是录音期间 `build()` 高频重建（每 100ms），当 `showDialog` push 新路由时，与 dialog 的 widget tree 过渡产生竞态，导致 InheritedWidget 依赖追踪进入不一致状态。

- **修复方案**：采用 **使用 `Overlay` 替代 `showDialog`** 的方案：
  1. `Overlay.insert()` 在当前路由上叠加 dialog，不会 push 新路由
  2. 避免了 widget tree 全局变动，不会触发 InheritedWidget 依赖冲突
  3. 保持 dialog 在当前页面上下文中，生命周期更可控

- **修改内容**：
  - **创建 `_EditTitleOverlay` Widget**（[`lib/pages/record/record_page.dart`](lib/pages/record/record_page.dart)）：
    - 使用 `OverlayEntry` 在当前路由上叠加对话框
    - 半透明遮罩 + 居中 Card 布局
    - 支持确认和取消操作，正确清理 OverlayEntry
    - 使用 `entryRef` 可空变量捕获，避免 `referenced_before_declaration` 错误

  - **修改 `_showEditTitleDialog()` 方法**：
    - 移除 `showDialog` 调用，改用 `_EditTitleOverlay.show()`
    - 移除所有 `debugPrint` 调试日志
    - 简化回调逻辑，在 `onConfirmed` 中直接调用 `setState` 更新标题

  - **移除调试日志**：
    - 清理 `initState()`、`dispose()`、`_startNewSession()`、`build()` 等方法中的 `debugPrint` 语句
    - 清理 `_showDiscardDialog()` 和 `_showStopConfirmDialog()` 中的调试日志

- **技术要点**：
  - `OverlayEntry` 的 `builder` 闭包中不能引用正在声明的变量，使用 `entryRef` 可空变量捕获
  - `Overlay` 不会创建新路由，因此不会触发路由过渡相关的 widget tree 重建
  - 对话框关闭时正确调用 `entry.remove()` 清理资源

- flutter analyze 通过（0 issues）

### 2026-04-29（修复录音页面默认标题与 session 标题不一致）

- **问题**：录音页面左上角默认显示"新建录音"，但 session 创建时的默认标题是"录音+时间"格式（如"录音 2026-04-29 18:24"），两者不一致
- **修复方案**：方案A — 将 `startSession()` 返回值从 `sessionId` 改为 `session.title`，录音页面使用返回的标题初始化 `_titleController`
- **修改文件**：
  - `lib/providers/recording_provider.dart`：`startSession()` 返回 `session.title` 而非 `sessionId`
  - `lib/pages/record/record_page.dart`：`_startNewSession()` 获取返回标题并设置到 `_titleController.text`
- flutter analyze 通过（0 issues）

### 2026-04-29（修复 Session ID 不一致导致录音名称无法持久化）

- **问题**：录音会话创建后，修改标题、更新时长等操作都无法持久化，所有通过 `recordingService.currentSessionId` 查找 session 的操作都找不到对应的 session
- **根本原因**：`RecordingService.startSession()` 内部使用 `_uuid.v4()` 生成新 UUID，与 `StorageService.createSession()` 创建的 session ID 不匹配
- **修复内容**：
  - `lib/services/recording_service.dart`：
    - `startSession()` 方法签名添加 `{required String sessionId}` 参数
    - 将 `_currentSessionId = _uuid.v4()` 替换为 `_currentSessionId = sessionId`
  - `lib/providers/recording_provider.dart`：
    - 调用 `startSession` 时传入 `session.sessionId` 参数
- flutter analyze 通过（0 issues）

### 2026-04-29（时间线事件显示改进与交互增强 — 设计方案）

- **需求背景**：录音时间线使用固定缩放阈值决定事件显示模式，不考虑实际间距导致重叠或浪费空间；录音时间线无事件点击交互；回放时间线事件点击只能查看不能编辑/删除
- **设计方案**：创建 `docs/timeline_event_improvement.md` 详细设计文档
- **核心改进**：
  - **重叠检测算法**：基于事件间实际像素间距的逐事件显示模式决策，替代固定全局阈值
  - **事件交互增强**：录音时间线添加事件点击；统一事件详情底部面板支持查看/编辑/删除
  - **边界情况处理**：同时间戳偏移、密集区域最小间距、编辑/删除后自动刷新
- **涉及文件**：
  - 新建：`lib/widgets/timeline/event_detail_panel.dart`
  - 修改：`recording_timeline.dart`、`playback_timeline.dart`、`timeline_event_item.dart`、`record_page.dart`、`playback_page.dart`、`recording_provider.dart`、`playback_provider.dart`
- **实现进度**：
  - [x] 录音时间轴重叠检测算法（`_calculateEventDisplayModes()`）
  - [x] 录音时间轴事件点击交互（`onEventTap` 回调 + `GestureDetector`）
  - [x] `EventDisplayMode` 枚举定义
  - [x] `_TapFeedbackWrapper` 点击视觉反馈组件
   - [x] 回放时间轴事件详情面板（`EventDetailPanel`）
   - [x] 回放时间轴事件编辑/删除功能
   - [x] `record_page.dart` 集成事件点击处理
   - [x] `playback_page.dart` 集成事件详情面板
   - [x] 照片/视频标题编辑和导出功能

### 2026-04-29（照片和视频标题编辑 + 导出功能）

- **需求背景**：照片和视频事件缺少标题显示和导出/分享功能
- **模型层**：
  - `photo.dart`：新增 `String? title` 字段，更新 `fromMap`/`toMap`/`copyWith`
  - `video_chunk.dart`：新增 `String? title` 字段，更新 `fromMap`/`toMap`/`copyWith`
  - `timeline_event.dart`：`fromPhoto()`/`fromVideo()` 新增 `title` 参数映射到 `label`
- **数据库层**：
  - `database_helper.dart`：会话数据库版本 1→2，`photos` 和 `video_chunks` 表添加 `title TEXT` 列，新增 `_onSessionUpgrade()` 迁移方法
  - `session_database.dart`：新增 `updatePhoto()`、`getVideoChunkById()`、`updateVideoChunk()` 方法，`getTimelineEvents()` 传递 `title`
- **服务层**：
  - `timeline_service.dart`：新增 `getPhotoById()`、`getVideoChunkById()`、`updatePhoto()`、`updateVideoChunk()` 方法
- **Provider 层**：
  - `recording_provider.dart`（`TimelineEventsNotifier`）：新增 `getPhotoById()`、`getVideoChunkById()`、`updatePhoto()`、`updateVideoChunk()`
  - `playback_provider.dart`（`PlaybackEventsNotifier`）：新增同样的4个方法
- **UI 层**：
  - `event_detail_panel.dart`：添加 `_exportMedia()` 导出方法（`Share.shareXFiles()`），扩展 `canEdit` 支持 photo/video，添加标题输入框
  - `record_page.dart`：`onEdit` 回调添加 photo/video 处理（获取原始对象 → `copyWith(title:)` → 更新）
  - `playback_page.dart`：`onEdit` 回调添加同样的 photo/video 处理
- **数据库迁移**：版本 1→2，`ALTER TABLE photos/video_chunks ADD COLUMN title TEXT`
- flutter analyze 通过（0 issues）

*最后更新：2026-04-29 by GDNDZZK*

### 2026-04-29（事件详情面板组件 + 编辑/删除交互）

- **新建 `lib/widgets/timeline/event_detail_panel.dart`**：
  - `EventDetailPanel` 通用底部面板组件，使用 `showModalBottomSheet` + `DraggableScrollableSheet`
  - 照片事件：显示缩略图/大图，点击全屏查看（`ImageViewer`），仅删除
  - 视频事件：显示缩略图，仅删除
  - 笔记事件：查看模式（标题+内容）+ 编辑模式（标题输入框+内容多行输入框）
  - 书签事件：查看模式（标签+颜色圆点）+ 编辑模式（标签输入框+8种预设颜色）
  - 录音事件：显示时长信息，不支持编辑/删除
  - 删除操作带二次确认（`AlertDialog`）
  - `EventDetailPanel.show()` 静态方法便捷调用

- **修改 `lib/providers/recording_provider.dart`**：
  - `TimelineEventsNotifier` 新增 `updateTextNote(TextNote)` 方法
  - `TimelineEventsNotifier` 新增 `updateBookmark(Bookmark)` 方法

- **修改 `lib/providers/playback_provider.dart`**：
  - `PlaybackEventsNotifier` 新增 `_sessionId` 和 `_database` 字段
  - `loadEvents()` 保存 `sessionId` 和 `database` 引用
  - 新增 `removeEvent()`、`updateTextNote()`、`updateBookmark()` 方法

- **修改 `lib/pages/record/record_page.dart`**：
  - `RecordingTimeline` 添加 `onEventTap` 回调
  - 新增 `_handleEventDetail()` 方法，调用 `EventDetailPanel.show()`

- **修改 `lib/pages/playback/playback_page.dart`**：
  - 替换4个独立事件处理方法为统一的 `_handleEventDetail()` 方法
  - 录音事件仅跳转播放位置，其他事件显示详情面板
  - 移除不再使用的 `_formatTimestamp()` 和 `_parseColor()` 方法

- flutter analyze 通过（0 issues）

